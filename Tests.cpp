#define CATCH_CONFIG_MAIN
#include "catch.h"

// this file has to exist for catch to work as it needs a main file, but all the
// tests are done in individual test files so nothing really happens here but it
// still needs to exist
